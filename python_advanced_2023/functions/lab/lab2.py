def multiply(num, b, *args):
    res = 1
    for num in args:
        res *= num
    return res


def Postgres(app, **kwargs)
    if kwargs:
        # make connection with user and apss
    else:
        # make connection


app = Flask()
db = Postgres(app, username = 'asd', password = 'asd')


