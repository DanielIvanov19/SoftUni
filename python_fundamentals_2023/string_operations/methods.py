# isdigit() -> checks if a character is a digit
print('1'.isdigit())

word = " He llo "
print(word.isalpha())
# but if there is a digit in the string, will return False

# isupper() when there is a digit doesn't change the result

print(word.rstrip())
# removes all spaces in the right position
# we can strip by a symbol example: '!' but if there are not
# any other symbols before or after them

# replace() -> replaces ALL occurencies of a word
# replace("cats", "dogs", 2) -> we can set the number of replaces





