from canvas import root
from authentication import render_entry


if __name__ == '__main__':  # if it has been run for the first time
    render_entry()  # render the entry
    root.mainloop()
